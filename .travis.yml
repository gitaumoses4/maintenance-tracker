language: python
python:
    - "2.7"
    - "3.6"
before_script:
    - cd API
install:
    - pip install -r API/requirements.txt
script:
    - cp .envexample .env
    - nosetests --with-coverage --cover-package=routes tests/*
# run coverage tests
after_success:
- coveralls
